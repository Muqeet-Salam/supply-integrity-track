# Backend example environment variables
# Copy to backend/.env and fill values before running

# Server
PORT=5000

# Blockchain / RPC
# e.g. http://127.0.0.1:8545 for local hardhat node or an Infura/Alchemy url
RPC_URL=http://127.0.0.1:8545

# Optional private keys (use with caution) for signing transactions server-side
MANUFACTURER_PRIVATE_KEY=
SUPPLIER_PRIVATE_KEY=

# Firebase (optional)
# Either set FIREBASE_SERVICE_ACCOUNT to the JSON service account string,
# or set GOOGLE_APPLICATION_CREDENTIALS to a file path on disk (not recommended for containers).
FIREBASE_SERVICE_ACCOUNT=
FIREBASE_DATABASE_URL=
GOOGLE_APPLICATION_CREDENTIALS=
